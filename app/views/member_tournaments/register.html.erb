<%= form_for(@member_tournament) do |f| %>
    <% if @member_tournament.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@member_tournament.errors.count, "error") %> prohibited this member_tournament from being saved:</h2>

          <ul>
            <% @member_tournament.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <%=link_to "< Back to tournaments", tournaments_path%>

    <div class="accountBlocks accountNewUser">
      <h4 class="blockhdng"><%=@tournament.title%></h4>
      <div class="inner formee">
        <div class="clearfix">
          <div class="twoThird">
            <label>Register As <em id="" class="formee-req">*</em></label>
            <%=f.text_field :memberId%>
          </div>
        </div>
      </div>
    </div>

<% end %>
